# Project Rules

- **Pushing to GitHub:** Do NOT automatically push changes to GitHub. ALWAYS wait for the user's explicit permission or request to push.
- **Versioning:** Whenever you make structural or meaningful updates to the mod, remember to automatically increment the `mod_version` in `gradle.properties` before proposing to push.
